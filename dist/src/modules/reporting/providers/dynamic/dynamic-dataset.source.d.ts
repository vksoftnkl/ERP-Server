import { OnModuleDestroy, OnModuleInit } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import { ConfiguredGridSqlService } from '../../../../common/configured-grid-sql/configured-grid-sql.service';
import { PgService } from '../../../../database/pg/pg.service';
import { PrismaService } from '../../../../database/prisma/prisma.service';
import { ProviderDescriptor } from '../report-data-provider.types';
import { SqlReportDatasetProvider } from './sql-report-dataset.provider';
export declare class DynamicDatasetSource implements OnModuleInit, OnModuleDestroy {
    private readonly prisma;
    private readonly pg;
    private readonly configuredGridSql;
    private readonly config;
    private readonly logger;
    private providers;
    private descriptors;
    private signature;
    private timer;
    constructor(prisma: PrismaService, pg: PgService, configuredGridSql: ConfiguredGridSqlService, config: ConfigService);
    onModuleInit(): Promise<void>;
    onModuleDestroy(): void;
    has(token: string): boolean;
    get(token: string): SqlReportDatasetProvider | undefined;
    list(docType?: string): ProviderDescriptor[];
    listTokens(): string[];
    invalidate(): Promise<void>;
    private refreshIfChanged;
    private readSignature;
    private refresh;
    private toDefinition;
}
