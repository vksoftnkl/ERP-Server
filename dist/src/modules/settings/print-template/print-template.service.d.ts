import { RequestContextService } from '../../../common/request-context/request-context.service';
import { PrismaService } from '../../../database/prisma/prisma.service';
import { AuditLogService } from '../../audit-log/audit-log.service';
import { ListPrintTemplateQueryDto } from './dto/list-print-template-query.dto';
import { SavePrintTemplateDto } from './dto/save-print-template.dto';
import { PrintTemplateDeleteResult, PrintTemplateListResult, PrintTemplatePayload } from './types/print-template-api.types';
export declare class PrintTemplateService {
    private readonly prisma;
    private readonly auditLogService;
    private readonly requestContextService;
    constructor(prisma: PrismaService, auditLogService: AuditLogService, requestContextService: RequestContextService);
    getTemplateById(ptlId: string, includeDeletedVersions?: boolean): Promise<PrintTemplatePayload>;
    listTemplates(query: ListPrintTemplateQueryDto): Promise<PrintTemplateListResult>;
    saveTemplate(dto: SavePrintTemplateDto): Promise<PrintTemplatePayload>;
    softDeleteTemplate(ptlId: string, modifiedBy?: string | null): Promise<PrintTemplateDeleteResult>;
    private planHeader;
    private assertCodeIsFree;
    private assertNotAssigned;
    private planVersions;
    private planFrozenVersion;
    private planVersionDelete;
    private designKeysPresent;
    private effectiveVersion;
    private planDatasets;
    private effectiveDataset;
    private planPointer;
    private applyVersionPlans;
    private createVersion;
    private updateVersion;
    private deleteVersion;
    private applyDatasetPlan;
    private createDataset;
    private updateDataset;
    private applyPointer;
    private findTemplate;
    private resolveWriteActor;
    private resolveAuditActor;
    private audit;
    private throwBadRequest;
    private throwConflict;
    private throwNotFound;
}
