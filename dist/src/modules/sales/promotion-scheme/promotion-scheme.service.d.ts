import { RequestContextService } from '../../../common/request-context/request-context.service';
import { PrismaService } from '../../../database/prisma/prisma.service';
import { AuditLogService } from '../../audit-log/audit-log.service';
import { SavePromotionSchemeBranchesDto } from './dto/save-promotion-scheme-branch.dto';
import { SavePromotionSchemeItemsDto } from './dto/save-promotion-scheme-item.dto';
import { SavePromotionSchemePartiesDto } from './dto/save-promotion-scheme-party.dto';
import { SavePromotionSchemeSlabsDto } from './dto/save-promotion-scheme-slab.dto';
import { SavePromotionSchemeDto } from './dto/save-promotion-scheme.dto';
import { PromotionSchemeBranchPayload, PromotionSchemeChildDeleteResult, PromotionSchemeDeleteResult, PromotionSchemeItemPayload, PromotionSchemePartyPayload, PromotionSchemePayload, PromotionSchemeSlabPayload } from './types/promotion-scheme-api.types';
export declare class PromotionSchemeService {
    private readonly prisma;
    private readonly auditLogService;
    private readonly requestContextService;
    constructor(prisma: PrismaService, auditLogService: AuditLogService, requestContextService: RequestContextService);
    saveScheme(dto: SavePromotionSchemeDto): Promise<PromotionSchemePayload>;
    getSchemeById(prmId: string): Promise<PromotionSchemePayload>;
    softDeleteScheme(prmId: string, modifiedBy?: string): Promise<PromotionSchemeDeleteResult>;
    private createScheme;
    private updateScheme;
    private applySchemeFields;
    private effectiveScheme;
    private assertSchemeInvariants;
    private assertCodeIsFree;
    saveBranches(dto: SavePromotionSchemeBranchesDto): Promise<PromotionSchemeBranchPayload[]>;
    getBranches(prmId: string): Promise<PromotionSchemeBranchPayload[]>;
    deleteBranch(prbId: string, modifiedBy?: string): Promise<PromotionSchemeChildDeleteResult>;
    private saveBranchRow;
    saveParties(dto: SavePromotionSchemePartiesDto): Promise<PromotionSchemePartyPayload[]>;
    getParties(prmId: string): Promise<PromotionSchemePartyPayload[]>;
    deleteParty(prpId: string, modifiedBy?: string): Promise<PromotionSchemeChildDeleteResult>;
    private savePartyRow;
    saveItems(dto: SavePromotionSchemeItemsDto): Promise<PromotionSchemeItemPayload[]>;
    getItems(prmId: string): Promise<PromotionSchemeItemPayload[]>;
    deleteItem(priId: string, modifiedBy?: string): Promise<PromotionSchemeChildDeleteResult>;
    private saveItemRow;
    private assertItemInvariants;
    saveSlabs(dto: SavePromotionSchemeSlabsDto): Promise<PromotionSchemeSlabPayload[]>;
    getSlabs(prmId: string): Promise<PromotionSchemeSlabPayload[]>;
    deleteSlab(prsId: string, modifiedBy?: string): Promise<PromotionSchemeChildDeleteResult>;
    private saveSlabRow;
    private assertSlabInvariants;
    private findSchemeWithChildren;
    private requireScheme;
    private assertNoDuplicates;
    private pickNumber;
    private resolveWriteActor;
    private resolveAuditActor;
    private audit;
    private throwBadRequest;
    private throwConflict;
    private throwNotFound;
}
