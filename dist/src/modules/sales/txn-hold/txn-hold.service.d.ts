import { ConfiguredGridListResult, ConfiguredGridSqlService } from '../../../common/configured-grid-sql/configured-grid-sql.service';
import { PrismaService } from '../../../database/prisma/prisma.service';
import { RequestContextService } from '../../../common/request-context/request-context.service';
import { AuditLogService } from '../../audit-log/audit-log.service';
import { ListTxnHoldQueryDto } from './dto/list-txn-hold-query.dto';
import { SaveTxnHoldDto } from './dto/save-txn-hold.dto';
import { TxnHoldConversion, TxnHoldDeleteResult, TxnHoldListItem, TxnHoldListMeta, TxnHoldLockScope, TxnHoldPayload } from './types/txn-hold-api.types';
export declare class TxnHoldService {
    private readonly prisma;
    private readonly auditLogService;
    private readonly configuredGridSqlService;
    private readonly requestContextService;
    constructor(prisma: PrismaService, auditLogService: AuditLogService, configuredGridSqlService: ConfiguredGridSqlService, requestContextService: RequestContextService);
    save(saveTxnHoldDto: SaveTxnHoldDto): Promise<TxnHoldPayload>;
    list(queryDto: ListTxnHoldQueryDto): Promise<ConfiguredGridListResult<TxnHoldListItem, TxnHoldListMeta>>;
    getById(txhId: string, txhAccYear?: string): Promise<TxnHoldPayload>;
    softDelete(txhId: string, txhAccYear?: string): Promise<TxnHoldDeleteResult>;
    resumeHold(txhId: string, deviceId: string | undefined | null, scope: TxnHoldLockScope): Promise<TxnHoldPayload>;
    releaseHold(txhId: string, deviceId: string | undefined | null, scope: TxnHoldLockScope): Promise<TxnHoldPayload>;
    forceReleaseHold(txhId: string, deviceId: string | undefined | null, scope: TxnHoldLockScope): Promise<TxnHoldPayload>;
    convertHold(txhId: string, deviceId: string | undefined | null, scope: TxnHoldLockScope, conversion: TxnHoldConversion): Promise<TxnHoldPayload>;
    private resolveResumeFailure;
    private resolveReleaseFailure;
    private resolveConvertFailure;
    private throwLockOwnershipFailure;
    private lockWhere;
    private readLocked;
    private pk;
    private emptyLease;
    private leaseExpiry;
    private ensureKindIsLeasable;
    private requireDeviceId;
    private requireOperator;
    private isUuid;
    private ensureConversionIsAllowed;
    private auditLockTransition;
    private createHold;
    private updateHold;
    private findLiveAutosave;
    private overwriteAutosave;
    private ensureScopeIsUnchanged;
    private ensureHoldNoIsUnique;
    private ensureHoldSlnoIsUnique;
    private ensureCompanyExists;
    private ensureBranchExists;
    private ensureDeviceExists;
    private ensureUserExists;
    private ensureSessionExists;
    private ensureStaffExists;
    private ensureValuesAreAllowed;
    private buildListWhere;
    private hasStructuredFilters;
    private mergedNumber;
    private parseDate;
    private requireField;
    private requirePayload;
    private requireAccYear;
    private handleWriteError;
    private throwNotFound;
    private displayName;
    private toPayload;
}
