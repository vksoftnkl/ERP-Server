import { RequestContextService } from '../../../common/request-context/request-context.service';
import { PrismaService } from '../../../database/prisma/prisma.service';
import { AuditLogService } from '../../audit-log/audit-log.service';
import { ListLoyaltySchemeQueryDto } from './dto/list-loyalty-scheme-query.dto';
import { SaveLoyaltySchemeDto } from './dto/save-loyalty-scheme.dto';
import { LoyaltySchemeDeleteResult, LoyaltySchemeEligibilityPayload, LoyaltySchemePayload } from './types/promotion-loyalty-points-api.types';
export declare class PromotionLoyaltyPointsService {
    private readonly prisma;
    private readonly auditLogService;
    private readonly requestContextService;
    constructor(prisma: PrismaService, auditLogService: AuditLogService, requestContextService: RequestContextService);
    saveScheme(dto: SaveLoyaltySchemeDto): Promise<LoyaltySchemePayload>;
    listSchemes(query: ListLoyaltySchemeQueryDto): Promise<LoyaltySchemePayload[]>;
    getSchemeById(lscId: string): Promise<LoyaltySchemePayload>;
    checkEligibility(lscId: string, cusId: string): Promise<LoyaltySchemeEligibilityPayload>;
    softDeleteScheme(lscId: string, modifiedBy?: string): Promise<LoyaltySchemeDeleteResult>;
    private createScheme;
    private updateScheme;
    private applySchemeFields;
    private effectiveScheme;
    private assertSchemeInvariants;
    private assertCodeIsFree;
    private assertPrimaryIsFree;
    private saveBranchRow;
    private savePartyRow;
    private saveItemRow;
    private saveSlabRow;
    private saveGiftRow;
    private assertBranchInvariants;
    private assertPartyInvariants;
    private assertItemInvariants;
    private assertSlabInvariants;
    private assertGiftInvariants;
    private findSchemeWithChildren;
    private requireScheme;
    private syncChildren;
    private softDeleteBranchRow;
    private softDeletePartyRow;
    private softDeleteItemRow;
    private softDeleteSlabRow;
    private softDeleteGiftRow;
    private assertNoDuplicates;
    private pickNumber;
    private pickNullableNumber;
    private resolveWriteActor;
    private resolveAuditActor;
    private audit;
    private throwBadRequest;
    private throwConflict;
    private throwNotFound;
}
