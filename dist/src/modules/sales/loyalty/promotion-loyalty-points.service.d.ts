import { RequestContextService } from '../../../common/request-context/request-context.service';
import { PrismaService } from '../../../database/prisma/prisma.service';
import { AuditLogService } from '../../audit-log/audit-log.service';
import { SaveLoyaltyGiftDto } from './dto/save-loyalty-gift.dto';
import { SaveLoyaltyPointDto } from './dto/save-loyalty-point.dto';
import { SaveLoyaltySchemeDto } from './dto/save-loyalty-scheme.dto';
import { LoyaltyGiftDeleteResult, LoyaltyGiftPayload, LoyaltyPointDeleteResult, LoyaltyPointPayload, LoyaltySchemeDeleteResult, LoyaltySchemePayload } from './types/promotion-loyalty-points-api.types';
export declare class PromotionLoyaltyPointsService {
    private readonly prisma;
    private readonly auditLogService;
    private readonly requestContextService;
    constructor(prisma: PrismaService, auditLogService: AuditLogService, requestContextService: RequestContextService);
    saveScheme(dto: SaveLoyaltySchemeDto): Promise<LoyaltySchemePayload>;
    getSchemeById(lsId: string): Promise<LoyaltySchemePayload>;
    softDeleteScheme(lsId: string, lsUpdatedBy?: string): Promise<LoyaltySchemeDeleteResult>;
    savePoint(dto: SaveLoyaltyPointDto): Promise<LoyaltyPointPayload>;
    getPointById(lsptId: string): Promise<LoyaltyPointPayload>;
    softDeletePoint(lsptId: string, lsptUpdatedBy?: string): Promise<LoyaltyPointDeleteResult>;
    saveGift(dto: SaveLoyaltyGiftDto): Promise<LoyaltyGiftPayload>;
    getGiftById(lsgId: string): Promise<LoyaltyGiftPayload>;
    softDeleteGift(lsgId: string, lsgUpdatedBy?: string): Promise<LoyaltyGiftDeleteResult>;
    private createScheme;
    private updateScheme;
    private calculatePointFactor;
    private createPoint;
    private updatePoint;
    private createGift;
    private updateGift;
    private findActiveSchemeWithChildren;
    private syncSchemeParties;
    private ensureSchemeExists;
    private getActiveScheme;
    private ensureItemExists;
    private ensureUnitExists;
    private ensurePointReferenceRecords;
    private ensureGiftReferenceRecords;
    private ensurePointScopeReference;
    private ensureSchemeCodeUnique;
    private ensurePointSlnoUnique;
    private ensureGiftSlnoUnique;
    private getNextPointSlno;
    private getNextGiftSlno;
    private resolveAuditActor;
    private throwBadRequest;
    private throwConflict;
    private throwNotFound;
    private buildErrorResponse;
}
